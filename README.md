
  # Build React Native Component

  This is a code bundle for Build React Native Component. The original project is available at https://www.figma.com/design/IFrYlt80TERsUF4khmCZco/Build-React-Native-Component.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  